{
  "rules": {
    "attendance": {
      ".read": true,
      ".write": true,
      "$record": {
        ".validate": "newData.hasChildren(['name', 'id', 'email', 'timestamp'])",
        "name": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "id": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "email": {
          ".validate": "newData.isString() && newData.val().matches(/^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$/)"
        },
        "timestamp": {
          ".validate": "newData.isString()"
        }
      }
    }
  }
}